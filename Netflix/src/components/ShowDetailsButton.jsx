import React from 'react';

const ShowDetailsButton = ({ onClick }) => {
	<button
		onClick={onClick}
		className='w-fullabsolute left-0 right-0 bottom-0 opacity-0 duration-[350ms] ease bg-transparent'
	>
		<span className='block w-[14px] mx-auto'></span>
	</button>;
};
